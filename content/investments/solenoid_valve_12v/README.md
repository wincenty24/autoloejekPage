# Elektrozawór 12 V — prosta instalacja przy kranie

Wpis w języku polskim i angielskim, gotowy do publikacji (`draft: false`).

- `data.json` — tytuł, opis i kolejność bloków.
- `12v-solenoid-valve-and-quick-coupling.png` — zdjęcie elektrozaworu i szybkozłączki; okładka wpisu.
- `pulse-water-meter-at-tap.jpg` — zdjęcie przepływomierza, wyświetlane z szerokością do 480 px.

Prześlij cały folder `solenoid_valve_12v` do skonfigurowanego folderu Dysku Google
(lub jego katalogu `investments`). Automatyczna publikacja wymaga aktywnej synchronizacji.
Kopia lokalna jest w `content/investments/solenoid_valve_12v` repozytorium strony.

Podgląd po `npm run check` i uruchomieniu serwera:
http://localhost:8080/pl/project-solenoid_valve_12v.html

Oryginalne zdjęcia w folderach Pobrane i Pulpit pozostawiono bez zmian.


Pochodzenie obrazów zapisano w `imageProvenance` w pliku `data.json`.
Strona wyświetla odpowiednie oznaczenia AI/Pexels przy obrazach.
Po podmianie zdjęcia sprawdź i zaktualizuj jego klasyfikację.
