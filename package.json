{
  "name": "autolejek-page",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --test scripts/investments.test.mjs scripts/sync-drive.test.mjs scripts/image-provenance.test.mjs && npm run build && node scripts/check.mjs",
    "manual:sync": "node scripts/generate-manual.mjs",
    "drive:sync": "node scripts/sync-drive.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
